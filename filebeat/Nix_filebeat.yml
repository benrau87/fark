filebeat:
  prospectors: 
  -
    paths:
    - /var/log/*.log
    - /var/log/syslog
    - /var/log/apache/httpd-*.log
    - /var/log/nginx/*.log
    input_type: log

registry_file: /var/lib/filebeat/registry
  
output:
  logstash:
    hosts: ["localhost:5044"]
  
