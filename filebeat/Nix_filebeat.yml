filebeat:
  prospectors: 
   -
    paths:
      - "/var/log/*.log"
      - "/var/log/syslog"
      - "/var/log/apache/httpd-*.log"
      - "/var/log/nginx/*.log"
    
output:
  logstash:
    hosts: ["localhost:5044"]
  
